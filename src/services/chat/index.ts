/**
 * Chat services — the agent runtime client (AgentService) and model
 * resolution (ModelService), plus their public types.
 *
 * Import chat services from `@services/chat` (or `@services/chat/AgentService`)
 * rather than reaching into page-local re-exports.
 */
export * from "./AgentService";
export * from "./ModelService";
