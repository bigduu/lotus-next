/**
 * Shared localStorage keys for theme persistence.
 *
 * Single source of truth so reads/writes stay consistent across
 * App bootstrap, the theme store, and the storage service.
 */
export const THEME_STORAGE_KEY = "copilot_ui_theme_v1";
